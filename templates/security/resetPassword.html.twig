<!DOCTYPE html>
<!--
Template Name: Tinker - HTML Admin Dashboard Template
Author: Left4code
Website: http://www.left4code.com/
Contact: muhammadrizki@left4code.com
Purchase: https://themeforest.net/user/left4code/portfolio
Renew Support: https://themeforest.net/user/left4code/portfolio
License: You must have a valid license purchased only from themeforest(the above link) in order to legally use the theme for your project.
-->
<html lang="en" class="light">
    <!-- BEGIN: Head -->
    <head>
        <meta charset="utf-8">
        {% block head %}
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    {% endblock %}
        <link href="{{asset('assetsAdmin/dist/images/logo.png')}}" rel="shortcut icon">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="Tinker admin is super flexible, powerful, clean & modern responsive tailwind admin template with unlimited possibilities.">
        <meta name="keywords" content="admin template, Tinker Admin Template, dashboard template, flat admin template, responsive admin template, web app">
        <meta name="author" content="LEFT4CODE">
        <title>Dashboard - XperienceZone</title>
        <!-- BEGIN: CSS Assets-->
        <link rel="stylesheet" href="{{asset('assetsAdmin/dist/css/app.css')}}" />
        <!-- END: CSS Assets-->
    </head>
    <!-- END: Head -->
    

{% block title %}Reset password{% endblock %}

{% block container %}
    <h2 class="text-lg font-medium mr-auto">Reset Your Password</h1>
    <br>
    <br>

    <div class="container">
        <section class="section register ">
            {% for message  in app.flashes('message') %}
                <div class="alert alert-danger">
                    {{ message }}
                </div>
            {% endfor %}
        </section>
        <section class="section register ">
            {% for danger  in app.flashes('danger') %}
                <div class="alert alert-info">
                    {{ danger }}
                </div>
            {% endfor %}
        </section>
        <section class="section register min-vh-100 d-flex flex-column align-items-center justify-content-center py-4">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-4 col-md-6 d-flex flex-column align-items-center justify-content-center">

                        <div class="d-flex justify-content-center py-4">
                            <a href="index.html" class="logo d-flex align-items-center w-auto">
                                <img src="assets/img/logo.png" alt="">
                                
                            </a>
                        </div><!-- End Logo -->

                        <div class="card mb-3">

                            <div class="card-body">

                                <form method="post">
                                <div class="col-12">
                                 <div class="search hidden sm:block">
                                    <input type="password" name="password" id="inputPass" class="form-class="search__input form-control border-transparent"" placeholder="New password..." required/>
                                    </div>
                                    <input type="hidden"  name="token" value={token}">

                                    <div class="invalid-feedback">Please enter a valid password!</div>
                                
                                <br><br>
                                <div class="col-12">
                                    <button class="btn btn-primary py-3 px-4 w-full xl:w-32 xl:mr-3 align-top" type="submit">Reset</button>
                                    <a href="{{ path('app_login') }}" class="btn btn-outline-secondary py-3 px-4 w-full xl:w-32 mt-3 xl:mt-0 align-top">back to login</a>

                                </div>

                                </form>
                            </div>
                        </div>

                        <div class="credits">
                            <!-- All the links in the footer should remain intact. -->
                            <!-- You can delete the links only if you purchased the pro version. -->
                            <!-- Licensing information: https://bootstrapmade.com/license/ -->
                            <!-- Purchase the pro version with working PHP/AJAX contact form: https://bootstrapmade.com/nice-admin-bootstrap-admin-html-template/ -->
                            
                        </div>

                    </div>
                </div>
            </div>

        </section>

    </div>

    
{% endblock %}
   
        
        <!-- BEGIN: JS Assets-->
        <script src="https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/markerclusterer.js"></script>
        <script src="https://maps.googleapis.com/maps/api/js?key=["your-google-map-api"]&libraries=places"></script>
        <script src="{{asset('assetsAdmin/dist/js/app.js')}}"></script>
        
<script>
    // Fonction pour mettre à jour l'heure actuelle
    function updateCurrentTime() {
        var currentTimeElement = document.getElementById('currentTime');
        if (currentTimeElement) {
            // Obtenir la date actuelle
            var currentDate = new Date();

            // Formatter l'heure au format HH:MM AM/PM
            var hours = currentDate.getHours() % 12 || 12;
            var minutes = currentDate.getMinutes();
            var ampm = hours >= 12 ? 'PM' : 'AM';
            
            // Mettre à jour le contenu de la balise avec l'heure formatée
            currentTimeElement.textContent = hours + ':' + (minutes < 10 ? '0' : '') + minutes + ' ' + ampm;
        }
    }

    // Mettre à jour l'heure initiale
    updateCurrentTime();

    // Mettre à jour l'heure chaque minute (60 000 millisecondes)
    setInterval(updateCurrentTime, 60000);
</script>
</html>