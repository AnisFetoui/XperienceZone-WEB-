{% extends 'base.html.twig' %}

{% block title %}Reclamations{% endblock %}

{% block body %}
    <h1>Reclamations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idr</th>
                <td>{{ reclamation.idr }}</td>
            </tr>
            <tr>
                <th>Daterec</th>
                <td>{{ reclamation.daterec ? reclamation.daterec|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Typerec</th>
                <td>{{ reclamation.typerec }}</td>
            </tr>
            <tr>
                <th>Refobject</th>
                <td>{{ reclamation.refobject }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamations_index') }}">back to list</a>

    <a href="{{ path('app_reclamations_edit', {'idr': reclamation.idr}) }}">edit</a>

    {{ include('reclamations/_delete_form.html.twig') }}
{% endblock %}
