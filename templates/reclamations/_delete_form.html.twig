<form method="post" action="{{ path('app_reclamations_delete', {'idr': reclamation.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.idr) }}">
    <button class="btn">Delete</button>
</form>
