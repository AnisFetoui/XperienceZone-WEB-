
<style>
    .transparent-button {
        background-color: black;
        float: right;
    }
</style>
<form method="post" action="{{ path('app_evenement_deleteback', {'idEvent': evenement.idEvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.idEvent) }}">
    <button class="btn btn transparent-button">Delete</button>
</form>