{% extends 'base.html.twig' %}

{% block title %}Channel{% endblock %}

{% block body %}
    <h1>Channel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCh</th>
                <td>{{ channel.idCh }}</td>
            </tr>
            <tr>
                <th>NomCh</th>
                <td>{{ channel.nomCh }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_channel_index') }}">back to list</a>

    <a href="{{ path('app_channel_edit', {'idCh': channel.idCh}) }}">edit</a>

    {{ include('channel/_delete_form.html.twig') }}
{% endblock %}
