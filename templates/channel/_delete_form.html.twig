<form method="post" action="{{ path('app_channel_delete', {'idCh': channel.idCh}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ channel.idCh) }}">
    <button class="btn">Delete</button>
</form>
