{% extends 'base.html.twig' %}

{% block title %}Produit {% endblock %}

{% block container %}
<<<<<<< HEAD
    <h1>Produit </h1>

    <!-- Swiper-->
    <div class="swiper-container swiper-slider swiper-slider_height-1 swiper-align-left swiper-align-left-custom context-dark bg-gray-darker" data-loop="false" data-autoplay="5500" data-simulate-touch="false" data-slide-effect="fade">
        <div class="swiper-wrapper">
            {% for produit in produits %}
                <div class="swiper-slide bg-dark">
                    <img class="swiper-image" src="{{ asset('uploads/images/' ~ produit.image) }}" alt="{{ produit.nomProd }}">
                    <div class="swiper-slide-caption">
                        <div class="container container-bigger swiper-main-section">
                            <div class="row row-fix justify-content-sm-center justify-content-md-start">
                                <div class="col-md-6 col-lg-5 col-xl-4 col-xxl-5">
                                    <h3>{{ produit.nomProd }}</h3>
                                    <div class="divider divider-decorate"></div>
                                    <p class="text-spacing-sm">{{ produit.descriptionProd }}</p>
                                    <p>Prix: {{ produit.prixProd }}</p>
                                    <p>Quantité: {{ produit.quantite }}</p>
                                    <a href="{{ path('app_produit_show', {'idProd': produit.idProd}) }}" class="button button-default-outline button-nina button-sm" style="padding: 5px 10px;">Voir les détails</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        <!-- Flèches de pagination -->
        <div class="swiper-button-next"></div>
        <div class="swiper-button-prev"></div>
        <!-- Swiper controls-->
        <div class="swiper-pagination-wrap">
            <div class="container container-bigger">
                <div class="row">
                    <div class="col-sm-12">
                        <div class="swiper-pagination"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bouton "Create New" -->
    <a href="{{ path('app_produit_new') }}" class="button button-default-outline button-nina button-sm" style="padding: 5px 10px;">Create New</a>

    <style>
        /* Styles spécifiques pour réduire la taille de l'image */
        .swiper-image {
            max-width: 50%; /* Ajustez en fonction de vos besoins */
            height: auto;
        }
    </style>
=======
    {# Formulaire de recherche stylé avec Bootstrap #}
   <form method="get" action="{{ path('app_produit_index') }}">
    <input type="text" name="nomProd" placeholder="Nom du produit" value="{{ nomProd }}">
    <input type="number" name="prixProd" placeholder="Prix maximum" value="{{ prixProd}}">
    <button type="submit">Rechercher</button>
</form>
    <a href="{{ path('app_produit_new') }}">Create new</a>
    
>>>>>>> f298a121a6cbb3b3d2874b1ea941eed10c309b82
{% endblock %}

{% block card %}
    <div id="resultats" class="row">
        {% for produit in produits %}
            <div class="col-md-4 col-sm-6">
                <div class="single-explore-item">
                    <div class="single-explore-img">
                        <img src="{{ asset('uploads/images/' ~ produit.image) }}" alt="Product Image">
                        <!-- Other content for produit -->
                        <div class="single-explore-img-info">
                            <button onclick="window.location.href='#'">best rated</button>
                            <div class="single-explore-image-icon-box">
                                <ul>
                                    <li>
                                        <div class="single-explore-image-icon">
                                            <a href="{{ path('app_produit_show', {'idProd': produit.idProd}) }}">
                                                <i class="fa fa-arrows-alt"></i>
                                            </a>
                                        </div>
                                    </li>
                                    <li>
                                        <div class="single-explore-image-icon">
                                            <i class="fa fa-bookmark-o"></i>
                                        </div>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <div class="single-explore-txt bg-theme-1">
                        <h2>
                            <a href="#">{{ produit.nomProd }}</a>
                        </h2>
                        <!-- Other content for produit -->
                        <p class="explore-rating-price">
                            {% if produit.noteProd is not null %}
                                <span class="explore-rating">{{ produit.noteProd }}</span>
                            {% else %}
                                <span class="explore-rating">Pas de note</span>
                            {% endif %}
                                Price
                                <span class="explore-price">{{ produit.prixProd }}</span>
                            </span>
                        </p>
                        <div class="explore-person">
                            <div class="row">
                                <div class="col-sm-10">
                                    <p>{{ produit.quantite }}...</p>
                                </div>
                            </div>
                        </div>
                        <div class="explore-open-close-part">
                            <div class="row">
                                <div class="col-sm-5">
                                    <a href="{{ path('app_produit_show', {'idProd': produit.idProd}) }}">Show Details</a>
                                </div>
                                <div class="col-sm-7">
                                    <div class="explore-map-icon">
                                        <a href="#"><i data-feather="map-pin"></i></a>
                                        <a href="#"><i data-feather="upload"></i></a>
                                        <a href="#"><i data-feather="heart"></i></a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="pagination text-center mt-4">
                               {{ knp_pagination_render(produits) }}
                               </div>
{% endblock %}

