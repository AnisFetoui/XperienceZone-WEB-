<form method="post" action="{{ path('app_message_delete', {'idMsg': message.idMsg}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.idMsg) }}">
    <button class="btn">Delete</button>
</form>
